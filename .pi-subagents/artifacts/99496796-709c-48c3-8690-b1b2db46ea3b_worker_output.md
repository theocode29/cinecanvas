Only my two intended files show as modified (`M`). The `??` untracked entries (`.pi-subagents/` runtime dir, `src-tauri/Cargo.lock`) are pre-existing and untouched by me.

---

## Summary

**Bug:** Tauri v2 `titleBarStyle: "Overlay"` + `hiddenTitle: true` makes the macOS window immovable (tauri#9503). Fix = explicit `data-tauri-drag-region` strip + `allow-start-dragging` permission.

**Files changed (2):**

`src/App.tsx` (final):
```tsx
export default function App() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', width: '100vw', height: '100vh', background: '#F2EDE4' }}>
      {/* Tauri v2 overlay windows need an explicit drag region (tauri#9503): this strip is the draggable titlebar; the native macOS traffic lights overlay it. */}
      <div data-tauri-drag-region style={{ height: 32, flexShrink: 0, userSelect: 'none' }} />
      {/* Canvas area — Phase 2 mounts React Flow here. */}
      <div style={{ flex: 1 }} />
    </div>
  );
}
```

`src-tauri/capabilities/default.json` permissions array:
```json
"permissions": [
  "core:default",
  "opener:default",
  "core:window:allow-start-dragging"
]
```

**Validation:**
- `pnpm exec tsc --noEmit` → exit 0, 0 errors.
- JSON parse of `default.json` → `JSON_VALID`.
- `git status` → only the 2 intended files modified; nothing staged.

**Unexpected:** nothing.

**Manual confirmation required:** the actual window-drag behavior can only be verified by you running `pnpm tauri dev` in a GUI session (per the project Gotcha, it cannot run here — blocks on GUI window + slow Rust compile). The 32px strip is seamless parchment (`#F2EDE4`); native traffic lights overlay its top-left.

**Risks:** none functional. The drag strip is visually seamless (same parchment color) and the canvas area (`flex: 1`) is reserved for Phase 2 as directed.